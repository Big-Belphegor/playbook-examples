worker_processes  1;
events {
    worker_connections  1024;
}

http {
    keepalive_timeout  65;

    server {
        listen       80;
        server_name  localhost;

        location /{{ url }} {
            root   html;
            index  index.html index.htm;
        }
    }
}
